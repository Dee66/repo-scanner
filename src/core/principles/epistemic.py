"""Epistemic principles for Repository Intelligence Scanner."""

EPISTEMIC_PRINCIPLES = [
    "only_claim_what_is_observable",
    "separate_evidence_from_judgment",
    "uncertainty_must_be_visible",
    "confidence_requires_justification",
    "silence_is_preferable_to_false_precision"
]

def check_only_claim_what_is_observable(claim: str) -> bool:
    """Ensure claims are based only on observable evidence."""
    return True  # Placeholder - allow

def check_separate_evidence_from_judgment(analysis: str) -> bool:
    """Ensure evidence is separated from judgment."""
    return True  # Placeholder - allow

def check_uncertainty_must_be_visible(assessment: str) -> bool:
    """Ensure uncertainty is explicitly visible."""
    return True  # Placeholder - allow

def check_confidence_requires_justification(confidence_level: str) -> bool:
    """Ensure confidence levels are justified."""
    return True  # Placeholder - allow

def check_silence_is_preferable_to_false_precision(precision_claim: str) -> bool:
    """Prefer silence over false precision claims."""
    return True  # Placeholder - allow
