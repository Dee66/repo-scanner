apiVersion: v1
kind: Secret
metadata:
  name: repo-scanner-secrets
  namespace: repo-scanner
type: Opaque
data:
  # Base64 encoded values - replace with actual secrets
  # API_KEY: <base64-encoded-api-key>
  # DATABASE_PASSWORD: <base64-encoded-password>
  # Add other secrets as needed